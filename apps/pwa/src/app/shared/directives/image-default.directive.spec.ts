import { ImageDefaultDirective } from './image-default.directive';

describe('ImageDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
